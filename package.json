
{
  "name": "truck-english-trainer",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm --workspace frontend run dev",
    "dev:backend": "echo 'Backend runs on serverless — no dev needed'",
    "build:frontend": "npm --workspace frontend run build"
  }
}
